<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration 
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- [1. 공통 설정] -->
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING" />
        <setting name="jdbcTypeForNull" value="NULL" />
    </settings>

    <!-- [2. DTO 별칭 자동 등록] -->
    <typeAliases>
        <package name="com.todayhospital.dto" />
    </typeAliases>

    <!-- [3. DB 연결 정보] -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <!-- 
                <property name="driver" value="oracle.jdbc.OracleDriver" />
                <property name="url" value="jdbc:oracle:thin:@112.220.114.31:1521:xe" />
                <property name="username" value="team1_202505m" />
                <property name="password" value="java" /> 
                -->
                <property name="driver" value="oracle.jdbc.OracleDriver" />
                <property name="url" value="jdbc:oracle:thin:@localhost:1521:xe" />
                <property name="username" value="week" />
                <property name="password" value="week" />
            </dataSource>
        </environment>
    </environments>

    <!-- [4. Mapper 자동 인식 등록] -->
    <!-- mybatis.config.xml 내부에 mapper 등록 -->
    <mappers>
      <mapper resource="mappers/MemberMapper.xml"/>
    </mappers>

    <!-- <mappers>
        <package name="com.todayhospital.mappers" />
    </mappers> -->

</configuration>
